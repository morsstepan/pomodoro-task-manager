import React from "react";
import { ServerTodo } from "../../apiClient";

interface TodoListProps {
  todos: ServerTodo[];
}

const TodoList: React.FC<TodoListProps> = () => null;

export default TodoList;
